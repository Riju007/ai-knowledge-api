[project]
name = "ai-knowledge-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "AVISEK DEY", email = "avisekdey1994@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
]

[project.scripts]
ai-knowledge-api = "ai_knowledge_api:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
    "ty>=0.0.84",
]
